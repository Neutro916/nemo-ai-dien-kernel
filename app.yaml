runtime: nodejs20

instance_class: F4
automatic_scaling:
  min_instances: 1
  max_instances: 10
  target_cpu_utilization: 0.65

env_variables:
  GCP_PROJECT_ID: "wide-maxim-487506-u1"
  PORT: "8080"
  VERCEL_GATEWAY_KEY: "ai_dark916"

handlers:
- url: /.*
  script: auto
  secure: always